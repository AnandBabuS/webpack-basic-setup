import jquery from 'jquery'
import lodash from 'lodash'

if(module.hot) { // this is to be used in NamedModulesPlugin to specify which file is getting hot reloaded
    module.hot.accept()
}

const data = 'hello world'
console.log(data)


